{
  "version": 2,
  "routes": [
    { "src": "/hello", "dest": "/api/hello.py" },
    { "src": "/run", "dest": "/api/app.py" },
    { "src": "/run-scheduled", "dest": "/api/run-scheduled.py" }
  ],
  "crons": [
    {
      "path": "/run-scheduled",
      "schedule": "*/10 * * * *"
    }
  ]
}
